{
  "name": "mac-synth",
  "version": "1.0.0",
  "description": "A Mac OS styled synthesizer prototype",
  "main": "index.html",
  "scripts": {
    "start": "http-server -p 8080 -o",
    "dev": "http-server -p 8080 -o",
    "build": "echo 'No build step needed'"
  },
  "keywords": [
    "synthesizer",
    "mac",
    "web audio"
  ],
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=14.x"
  }
} 